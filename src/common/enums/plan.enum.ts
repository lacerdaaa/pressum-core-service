export enum UserPlan {
  FREE = 'free',
  PREMIUM = 'premium',
  INTENSIVE = 'intensive',
}

export enum PlanStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  CANCELED = 'canceled',
  EXPIRED = 'expired',
}
